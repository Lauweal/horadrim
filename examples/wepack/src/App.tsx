import React from 'react';
import { Button } from '@horadrim/components';
const App: React.FC = () => {
  return (
    <div className="app">
        <Button />
      <h1>React + TypeScript + Webpack + Babel + Less</h1>
    </div>
  );
};

export default App;