import React from 'react';
import { Button, Input } from '@horadrim/components';
const App: React.FC = () => {
  return (
    <div className="app">
        <Button />
        <Input />
      <h1>React + TypeScript + Webpack + Babel + Less</h1>
    </div>
  );
};

export default App;